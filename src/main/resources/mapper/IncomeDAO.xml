<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.textin.dal.dao.IncomeDAO">

    <resultMap id="resultMap" type="org.textin.dal.dataobject.IncomeDO">
        <id column="id" property="id"/>
        <id column="is_delete" property="isDelete"/>
        <id column="create_at" property="createAt"/>
        <id column="update_at" property="updateAt"/>

        <result column="amount" property="amount"/>
        <result column="category" property="category"/>
        <result column="subcategory" property="subcategory"/>
        <result column="comment" property="comment"/>
        <result column="creatorId" property="creatorId"/>
        <result column="ledgerId" property="ledgerId"/>
    </resultMap>
    <sql id="allColumn">id, is_delete, create_at, update_at,amount,category,subcategory,comment,creator_id,ledger_id</sql>
    <sql id="tableName">income</sql>

    <sql id="where_clause_do" >
        <trim prefix="where" prefixOverrides="and|or" >
            is_delete = 0
            <if test="subcategory != null">
                and subcategory = #{subcategory}
            </if>
            <if test="comment != null">
                and comment = #{comment}
            </if>
        </trim>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyColumn="id" keyProperty="id" parameterType="org.textin.dal.dataobject.IncomeDO">
        insert into <include refid="tableName"/>
            (create_at,amount,creator_id,ledger_id,category,subcategory,comment)
        values
        (
        #{createAt},
        #{amount},
        #{creatorId},
        #{ledgerId},
        #{category},
        #{subcategory},
        #{comment}
        )
    </insert>
    <update id="update"></update>
</mapper>